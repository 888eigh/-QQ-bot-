# QQ Bot 详细注释文档

> 基于 NoneBot2 + NapCat + DeepSeek V4 Flash 的 QQ 机器人
> 版本：v2.0 | 最后更新：2026-08-23

---

## 一、项目总览

### 1.1 技术栈

| 组件 | 技术 | 版本 | 作用 |
|------|------|------|------|
| 机器人框架 | NoneBot2 | 2.5.0 | 异步事件驱动框架，插件化架构 |
| QQ 协议端 | NapCat | - | 基于 LiteLoaderQQNT，提供 OneBot V11 协议 |
| AI 模型 | DeepSeek V4 Flash | - | 对话生成，思考模式可关闭 |
| Web 驱动 | FastAPI + Uvicorn | - | NoneBot 的 ASGI 服务端 |
| HTTP 客户端 | httpx | - | 异步调用 AI API |
| GUI | tkinter | 内置 | 图形管理面板 |

### 1.2 架构图

```
用户消息 → NapCat(QQ协议端) → WebSocket → NoneBot2
                                              ↓
                                    插件分发系统
                                    ├── chat.py        (AI对话)
                                    ├── command_reply.py (文档指令)
                                    ├── text_docs.py    (text文档指令)
                                    ├── active_send.py  (主动消息)
                                    └── ping.py         (测试)
                                              ↓
                                    核心模块层
                                    ├── ai_chat.py      (AI调用+动态prompt)
                                    ├── affinity.py     (好感度系统)
                                    ├── personality.py  (性格系统)
                                    ├── media_manager.py(图片表情)
                                    ├── text_docs.py    (text文档)
                                    ├── active_sender.py(主动发送)
                                    ├── port_manager.py (自适应端口)
                                    ├── napcat_config.py(配置同步)
                                    ├── doc_replier.py  (JSON指令库)
                                    └── logger.py       (UTF-8日志)
```

### 1.3 目录结构

```
qq-bot/
├── main.py                  # 统一入口（auto/bot/gui/napcat/status）
├── bot.py                   # NoneBot 启动入口
├── napcat_launcher.py       # NapCat 进程管理
├── boring.txt               # 主动消息库（%分隔）
├── pyproject.toml           # NoneBot 插件/适配器注册
├── requirements.txt         # Python 依赖
├── qq_bot.spec              # PyInstaller 打包配置
├── build_exe.bat            # Windows 一键打包脚本
├── BUILD.md                 # 打包说明
│
├── config/
│   ├── settings.py          # 配置管理（JSON持久化，热更新）
│   ├── config.json          # 运行时配置
│   └── docs/
│       └── commands.json    # /XXX 文档指令库
│
├── core/                    # 核心业务模块
│   ├── __init__.py          # 统一导出
│   ├── logger.py            # UTF-8 日志系统
│   ├── ai_chat.py           # AI 聊天管理器
│   ├── port_manager.py      # 自适应端口
│   ├── doc_replier.py       # JSON 指令回复
│   ├── napcat_config.py     # NapCat 配置自动修改
│   ├── text_docs.py         # text 目录文档指令
│   ├── personality.py       # 性格设定系统
│   ├── affinity.py          # 好感度计算系统
│   ├── media_manager.py     # 图片/表情管理
│   └── active_sender.py     # 不定时主动消息
│
├── gui/
│   ├── __init__.py
│   └── app.py               # tkinter 双页面管理面板
│
├── plugins/                 # NoneBot 插件（事件响应层）
│   ├── __init__.py
│   ├── chat.py              # AI 对话 + 性格/好感度/多媒体
│   ├── command_reply.py     # /XXX JSON 指令回复
│   ├── text_docs.py         # #文档名 text 文档指令
│   ├── active_send.py       # /boring 主动消息控制
│   └── ping.py              # /ping /echo 测试
│
├── text/                    # 文本文档库（#指令读取）
│   ├── intro.txt
│   └── guide.txt
├── picture/                 # AI 可调用的图片
├── emotional/               # AI 可调用的表情
├── data/                    # 运行时数据
│   ├── affinity.json        # 好感度数据
│   ├── personality.json     # 性格设定
│   └── active_send.json     # 主动消息配置
└── logs/                    # UTF-8 日志
    ├── bot_YYYY-MM-DD.log   # 全量日志
    ├── error_YYYY-MM-DD.log # 错误日志
    └── api_YYYY-MM-DD.log   # API 专用日志
```

---

## 二、入口程序详解

### 2.1 main.py — 统一启动入口

**功能**：根据命令行参数选择启动模式，是程序的总入口。

**核心代码段说明**：

```python
def main():
    parser = argparse.ArgumentParser(...)
    parser.add_argument("mode", choices=["auto","bot","gui","napcat","status"])
    args = parser.parse_args()
```
- 使用 argparse 解析命令行参数
- 5 种模式：auto（自动检测）、bot（命令行启动）、gui（图形界面）、napcat（仅启动NapCat）、status（查看状态）

```python
def has_gui_display() -> bool:
    if sys.platform in ("win32", "darwin"):
        return True
    return bool(os.environ.get("DISPLAY"))
```
- 检测是否有图形显示环境
- Windows/macOS 默认有，Linux 检查 DISPLAY 环境变量
- auto 模式下据此决定启动 GUI 还是命令行

```python
def run_bot():
    port = port_manager.get_port()       # 自适应端口
    start_napcat_if_configured()         # 可选自动启动NapCat
    import bot
    bot.main()                           # 进入NoneBot主循环
```
- bot 模式：先获取可用端口，可选启动NapCat，然后进入NoneBot

### 2.2 bot.py — NoneBot 启动入口

**功能**：初始化 NoneBot，注册适配器和插件，启动 WebSocket 服务。

**核心代码段说明**：

```python
def setup_environment():
    port = port_manager.get_port()                    # 1.自适应端口
    nc_result = napcat_config.auto_configure(port)    # 2.首次启动改NapCat配置
    os.environ["PORT"] = str(port)                    # 3.设置环境变量
    os.environ["DRIVER"] = "~fastapi"
```
- 启动前三步：端口分配 → NapCat配置同步 → 环境变量注入

```python
nonebot.init()                           # 初始化NoneBot
driver = nonebot.get_driver()            # 获取驱动
driver.register_adapter(OneBotV11Adapter) # 注册OneBot V11适配器
nonebot.load_from_toml("pyproject.toml") # 从toml加载插件
nonebot.run()                            # 启动uvicorn服务
```
- NoneBot 标准启动流程
- OneBot V11 适配器负责与 NapCat 通信

### 2.3 napcat_launcher.py — NapCat 进程管理器

**功能**：启动/停止/重启 NapCat 进程，监控运行状态。

**核心类**：`NapCatLauncher`

```python
def detect_napcat(self) -> Optional[str]:
    candidates = ["/opt/NapCat/napcat", "C:\\Program Files\\NapCat\\..."]
    for path in candidates:
        if Path(path).exists():
            return path
```
- 自动检测 NapCat 安装路径，支持 Linux/Windows/macOS 常见路径

```python
def start(self) -> bool:
    self.process = subprocess.Popen(cmd, stdout=PIPE, stderr=STDOUT, ...)
    time.sleep(3)
    return self.is_running()
```
- 用 subprocess.Popen 启动 NapCat，等待3秒确认启动成功
- 输出重定向到管道，可通过 get_output() 读取

---

## 三、配置模块详解

### 3.1 config/settings.py — 全局配置管理

**功能**：单例模式管理所有配置，JSON 文件持久化，支持热更新。

**核心设计**：

```python
class Settings:
    _instance = None
    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance
```
- 单例模式，全局只有一个配置实例
- 任何模块 `from config import settings` 都拿到同一个对象

```python
default = {
    "api_base_url": "https://api.deepseek.com",
    "api_model": "deepseek-v4-flash",
    "api_thinking_disabled": True,
    "bot_port": 8080,
    "auto_port": True,
    "text_command_format": "hash",
    ...
}
```
- 默认配置字典，包含 API、Bot、NapCat、日志、聊天等所有参数
- 新增配置项只需在这里加 key，自动持久化

```python
def get(self, key, default=None):
    return self._config.get(key, default)

def set(self, key, value):
    self._config[key] = value
    self._save_config()
```
- get/set 接口，set 自动保存到 config.json
- GUI 修改配置后调用 set 即持久化

---

## 四、核心模块详解

### 4.1 core/logger.py — UTF-8 日志系统

**功能**：按日分文件的 UTF-8 日志，三类日志分离，自动清理过期日志。

**核心设计**：

```python
class UTF8FileHandler(logging.FileHandler):
    def __init__(self, filename, ...):
        super().__init__(filename, encoding="utf-8", ...)
```
- 继承 logging.FileHandler，强制 encoding="utf-8"
- 确保中文日志不会乱码

```python
# 三类日志文件
bot_YYYY-MM-DD.log     # 全量日志（DEBUG及以上）
error_YYYY-MM-DD.log   # 仅错误（ERROR及以上）
api_YYYY-MM-DD.log     # API专用（调用记录、余额警告）
```
- 全量日志用于排查问题
- 错误日志快速定位异常
- API 日志监控调用量和余额

```python
def api_balance_warning(self, msg):
    self.api_logger.warning(f"[API余额/配额] {msg}")
    self.logger.warning(f"API余额/配额警告: {msg}")
```
- 专门的余额警告方法，同时写入 API 日志和主日志
- 在 ai_chat.py 中遇到 402/429 状态码时调用

```python
def check_and_rotate(self):
    today_file = self.logs_dir / f"bot_{self._today_str()}.log"
    if not today_file.exists():
        # 移除旧handler，重新挂载新日期的文件
        ...
```
- 每次写日志前检查是否跨天，跨天自动切换到新日期的文件
- 实现按日轮转，无需外部定时任务

### 4.2 core/ai_chat.py — AI 聊天管理器

**功能**：调用 DeepSeek API 进行多轮对话，动态注入性格/好感度/媒体资源。

**核心类**：`AIChatManager` + `ChatSession`

```python
class ChatSession:
    def __init__(self, user_id, system_prompt, history_limit=20):
        self.messages = [{"role": "system", "content": system_prompt}]
```
- 每个用户一个会话，维护消息历史
- history_limit 控制保留多少轮对话，防止 token 溢出

```python
def build_system_prompt(self, user_id) -> str:
    parts = []
    parts.append(personality.get_system_prompt(user_id))  # 性格
    parts.append(f"【好感度】{affinity.get_affinity_hint(user_id)}")  # 好感度
    parts.append(f"【媒体资源】{media_manager.get_media_list_text()}")  # 图片表情
    return "\n\n".join(parts)
```
- **核心方法**：每次对话前动态构建 system prompt
- 三段式：性格设定 + 好感度态度 + 可用媒体资源
- 性格决定说话风格，好感度决定态度，媒体资源让AI知道能发什么图

```python
def _update_system_prompt(self, session, user_id):
    new_prompt = self.build_system_prompt(user_id)
    session.messages[0]["content"] = new_prompt
```
- 每次发消息前更新 session 的 system 消息
- 确保性格切换和好感度变化即时生效

```python
payload = {
    "model": self.model,                    # deepseek-v4-flash
    "messages": session.messages,
    "max_tokens": self.max_tokens,
    "temperature": self.temperature,
    "thinking": {"type": "disabled"},       # 关闭思考模式
}
```
- DeepSeek API 请求体
- `thinking: {"type": "disabled"}` 关闭思考模式，响应更快、成本更低

```python
if response.status_code == 401: return "API Key无效"
if response.status_code == 402: bot_logger.api_balance_warning("余额不足")
if response.status_code == 429: bot_logger.api_balance_warning("配额耗尽")
```
- 完整的 HTTP 状态码处理
- 401/402/429/5xx 分别给出用户友好提示和日志记录

### 4.3 core/affinity.py — 好感度系统

**功能**：每次对话后异步调用 API 评估好感度变化，持久化存储，影响 AI 回复态度。

**核心设计**：

```python
async def update_affinity(self, user_id, user_message, ai_reply):
    prompt = (
        f"你是情感分析专家。当前好感度: {current_score}\n"
        f"用户说: {user_message[:200]}\n"
        f"AI回复: {ai_reply[:200]}\n"
        f"请只回复一个整数(-10到+10)，表示好感度变化。"
    )
```
- 用 AI 自己评估对话对好感度的影响
- 截取消息前200字，控制 token 消耗
- 要求只回复数字，便于解析

```python
numbers = re.findall(r"-?\d+", delta_text)
if numbers:
    delta = max(-10, min(10, int(numbers[0])))
    self._apply_delta(user_id, delta)
```
- 用正则提取数字，限制在 -10~+10 范围
- 防止单次对话好感度剧变

```python
def get_level(self, score) -> str:
    if score >= 80: return "挚爱"
    elif score >= 60: return "喜欢"
    elif score >= 40: return "好感"
    elif score >= 20: return "友善"
    elif score >= 0: return "普通"
    elif score >= -20: return "冷淡"
    elif score >= -40: return "厌恶"
    else: return "仇恨"
```
- 8 级好感度，从挚爰到仇恨
- 每个等级对应不同的 AI 回复态度提示

```python
asyncio.create_task(affinity.update_affinity(user_id, text, reply))
```
- 在 chat.py 中用 create_task 异步执行
- 不阻塞用户收到回复，好感度计算在后台进行

### 4.4 core/personality.py — 性格设定系统

**功能**：7 种预设性格 + 自定义性格，per-user 独立设定，注入 system prompt。

**预设性格**：

| 性格 | 特点 | system prompt 要点 |
|------|------|-------------------|
| 傲娇 | 口是心非 | "哼""才不是呢""笨蛋"，表面冷淡内心温柔 |
| 温柔 | 体贴善解人意 | "呢""呀""哦"，耐心温暖 |
| 毒舌 | 尖酸刻薄 | 犀利吐槽，指出问题，内心不坏 |
| 元气 | 活力四射 | "哇！""冲冲冲！"，感叹号多 |
| 冷淡 | 话少冷漠 | 简短回复，偶尔流露出关心 |
| 病娇 | 偏执占有欲 | 可爱与恐怖切换，阴暗想法 |
| 默认 | 友好平衡 | 简洁自然，乐于助人 |

```python
def get_system_prompt(self, user_id) -> str:
    name = self.get_user_personality(user_id)  # per-user优先，否则全局
    if name in PRESET_PERSONALITIES:
        return PRESET_PERSONALITIES[name]["system_prompt"]
    return self._data["custom"].get(name, {}).get("system_prompt", "")
```
- 用户可设定独立性格，未设定则用全局性格
- 自定义性格存在 personality.json

### 4.5 core/media_manager.py — 图片/表情管理

**功能**：扫描 picture/ 和 emotional/ 目录，AI 回复中用标记引用，插件解析后发送图片。

**核心机制**：

```python
# AI 在回复中使用标记
"你好呀！[pic:happy.jpg] 今天真开心[emo:smile.gif]"

# 插件解析标记
def parse_and_get_media(self, text):
    clean_text = self.pic_pattern.sub("", text)   # 移除标记
    media_files = [self.get_picture_path(m) for m in matches]
    return clean_text, media_files
```
- AI 从 prompt 中得知可用图片列表，选择合适的用 `[pic:文件名]` 标记
- 插件解析标记，移除文本中的标记，提取图片路径
- 用 OneBot V11 的 `MessageSegment.image(file://路径)` 发送

```python
def get_media_list_text(self) -> str:
    pics = self.list_pictures()    # 扫描picture目录
    emos = self.list_emotionals()  # 扫描emotional目录
    return "可用图片: ...\n可用表情: ..."
```
- 生成媒体列表文本，注入到 AI 的 system prompt
- AI 据此知道能发哪些图片，最多列20个防止 prompt 过长

### 4.6 core/text_docs.py — text 目录文档指令

**功能**：识别自定义格式指令（默认 `#文档名`），读取 text/ 目录对应文件回复。

```python
def get_command_pattern(self) -> re.Pattern:
    fmt = settings.get("text_command_format", "hash")
    patterns = {
        "hash": r"#(\w+)",        # #文档名
        "bracket": r"\[(\w+)\]",  # [文档名]
        "brace": r"\{\{(\w+)\}\}", # {{文档名}}
        "at": r"@(\w+)",          # @文档名
    }
```
- 4 种预设格式，也支持用户自定义正则
- 修改配置即可切换指令格式，无需改代码

```python
def match_and_read(self, message):
    match = self.get_command_pattern().search(message)
    if match:
        doc_name = match.group(1)
        content = self.read_doc(doc_name)  # 读取text/目录
        return (doc_name, content)
```
- 从消息中正则匹配指令，读取对应 .txt/.md 文件
- 支持缓存，重复读取不重复读盘

### 4.7 core/active_sender.py — 不定时主动消息

**功能**：从 boring.txt 随机选句，随机间隔主动发送到指定群。

```python
def load_messages(self) -> list[str]:
    content = self.boring_file.read_text(encoding="utf-8")
    return [m.strip() for m in content.split("%") if m.strip()]
```
- 用 `%` 分隔每句话，过滤空行
- boring.txt 中每句话用 % 隔开

```python
async def _loop(self):
    while self._running and self.enabled:
        wait_time = random.randint(min_interval, max_interval)
        # 分段等待，支持快速停止
        while waited < wait_time and self._running:
            await asyncio.sleep(10)
            waited += 10
        await self._send_one()
```
- 主循环：随机等待 → 发送 → 再随机等待
- 分段 sleep（每10秒检查一次），确保 stop 命令能快速响应

```python
@driver.on_bot_connect
async def _on_bot_connect(bot):
    active_sender.set_bot(bot)
    if active_sender.enabled:
        active_sender.start()
```
- Bot 连接 NapCat 时自动设置实例并启动
- Bot 断开时自动停止，防止发送失败

### 4.8 core/port_manager.py — 自适应端口

```python
def get_port(self) -> int:
    if not self.is_port_in_use(self.preferred_port):
        return self.preferred_port
    if self.auto_port:
        return self.find_available_port()  # 范围扫描
    return self.preferred_port  # 不开启自适应则报错
```
- 首选端口被占用时，在 8080~9090 范围扫描
- 全部占用则让系统随机分配
- 确定后回写到配置文件

### 4.9 core/napcat_config.py — NapCat 配置自动修改

```python
def update_reverse_ws_port(self, config_path, bot_port):
    config = json.load(open(config_path))
    # 格式1: network.websocketClients[].url
    if "network" in config and "websocketClients" in config["network"]:
        for client in config["network"]["websocketClients"]:
            client["url"] = f"ws://127.0.0.1:{bot_port}/onebot/v11/ws"
    # 格式2/3/4: 兼容其他配置结构
    ...
```
- 首次启动时自动检测 NapCat 配置文件
- 备份原配置，修改反向 WebSocket URL 端口与 Bot 一致
- 支持多种 NapCat 版本的配置格式

### 4.10 core/doc_replier.py — JSON 指令回复库

```python
def get_reply(self, command) -> Optional[str]:
    return self.commands.get(command.lower())
```
- 从 config/docs/commands.json 加载指令
- 用户发 /hello → 查找 hello 对应的内容回复
- 支持热重载、增删改查

---

## 五、GUI 界面详解

### gui/app.py — 双页面管理面板

**功能页（功能模板）**：
- 6 种模板：欢迎语、群规、关于机器人、帮助说明、AI人设、自定义指令
- 下拉选择模板 → 编辑内容 → 应用模板自动生成 /XXX 指令
- AI人设模板直接修改 system prompt

**设置页（高级设置）**：
- 可滚动区域，5 大配置区
- API 配置：地址、Key、模型、超时、温度、思考模式开关
- Bot 配置：主机、端口、自适应开关、管理员、昵称
- NapCat 配置：路径、配置文件、自动启动
- 日志配置：级别、保留天数
- 聊天配置：总开关、群聊/私聊开关、历史条数
- 底部按钮：保存设置、测试API、重新配置NapCat

```python
def _build_window(self):
    self.root.geometry("850x650")
    self.root.minsize(700, 500)
    notebook = ttk.Notebook(self.root)
    self._tab_features(notebook)   # 功能页
    self._tab_settings(notebook)   # 设置页
```
- 自适应窗口，最小尺寸 700x500
- Notebook 双标签页布局

---

## 六、插件详解

### 6.1 plugins/chat.py — AI 对话插件

**触发方式**：私聊直接发消息 / 群聊@机器人 / `/chat <内容>`

**处理流程**：
1. 提取纯文本消息
2. 调用 `ai_chat.chat(user_id, text)` 获取 AI 回复
3. 异步触发 `affinity.update_affinity()` 更新好感度
4. 调用 `_send_reply()` 解析媒体标记并发送

```python
async def _send_reply(event, text):
    clean_text, media_files = media_manager.parse_and_get_media(text)
    msg = Message()
    if clean_text: msg.append(MessageSegment.text(clean_text))
    for path in media_files:
        msg.append(MessageSegment.image(f"file://{path.absolute()}"))
    await msg
```
- 核心回复方法：解析媒体标记 → 构造消息 → 发送
- 群聊自动添加引用回复

**命令列表**：
| 命令 | 功能 |
|------|------|
| `/chat <内容>` | 主动AI对话 |
| `/clear` | 清除历史+好感度 |
| `/personality [性格]` | 查看/切换性格 |
| `/affinity` | 查看好感度 |
| `/media` | 查看可用图片表情 |

### 6.2 plugins/command_reply.py — /XXX JSON 指令

**触发方式**：任何以 `/` 开头的消息，匹配 commands.json 中的指令名

```python
@doc_matcher.handle()
async def handle_doc_command(event):
    command = raw_msg[1:].split(maxsplit=1)[0].lower()
    reply = doc_replier.get_reply(command)
    if reply: await doc_matcher.finish(reply)
```
- 优先级 5（低于专门注册的命令），不会拦截 /ping /chat 等
- 匹配成功直接回复文档内容

### 6.3 plugins/text_docs.py — #文档名 text 指令

**触发方式**：消息中包含 `#文档名`（格式可配置）

```python
result = text_docs.match_and_read(raw)
if result:
    doc_name, content = result
    await text_matcher.finish(content)
```
- 自动匹配自定义格式指令
- `/text <名>` 也可直接读取
- `/textlist` 列出所有文档

### 6.4 plugins/active_send.py — 主动消息控制

| 命令 | 功能 | 权限 |
|------|------|------|
| `/boring status` | 查看状态 | 所有人 |
| `/boring on/off` | 开关 | 管理员 |
| `/boring now` | 立即发一条 | 管理员 |
| `/boring set <群号>` | 设置目标群 | 管理员 |
| `/boring interval <min> <max>` | 设置间隔 | 管理员 |
| `/boring reload` | 重载boring.txt | 管理员 |

### 6.5 plugins/ping.py — 测试插件

- `/ping` → 返回 pong + 用户信息
- `/echo <内容>` → 回声测试

---

## 七、数据流转详解

### 7.1 一次 AI 对话的完整流程

```
1. 用户发消息（私聊或@群聊）
   ↓
2. NoneBot 事件分发 → chat.py 的 on_message 匹配
   ↓
3. _handle_chat() 被调用
   ↓
4. ai_chat.chat(user_id, text)
   ├─ 获取/创建 ChatSession
   ├─ _update_system_prompt() 动态构建prompt
   │  ├─ personality.get_system_prompt()  性格
   │  ├─ affinity.get_affinity_hint()     好感度态度
   │  └─ media_manager.get_media_list_text()  可用图片
   ├─ 构造请求体（含 thinking:disabled）
   ├─ httpx 异步调用 DeepSeek API
   ├─ 处理状态码（401/402/429/5xx）
   └─ 返回 AI 回复文本
   ↓
5. asyncio.create_task(affinity.update_affinity())
   └─ 后台异步调用API评估好感度变化，更新存储
   ↓
6. _send_reply()
   ├─ media_manager.parse_and_get_media() 解析[pic:][emo:]标记
   ├─ 构造 Message（文本+图片）
   └─ 发送到 QQ
   ↓
7. 用户收到回复（可能包含图片）
```

### 7.2 好感度如何影响回复

```
用户对话 → affinity.update_affinity() → API评估变化量
    ↓
affinity.json 更新分数
    ↓
下次对话 build_system_prompt() 读取当前分数
    ↓
get_affinity_hint() 根据等级返回态度提示
    ↓
注入 system prompt → AI 按对应态度回复
```

### 7.3 主动消息流程

```
Bot连接NapCat → on_bot_connect → active_sender.start()
    ↓
_loop() 随机等待 1800~7200 秒
    ↓
_send_one() → random_message() 从boring.txt选一句
    ↓
bot.send_group_msg() 发送到目标群
    ↓
更新计数和时间 → 继续循环
```

---

## 八、配置说明

### 8.1 config.json 主要配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| api_base_url | https://api.deepseek.com | API 地址 |
| api_key | "" | DeepSeek API 密钥 |
| api_model | deepseek-v4-flash | 模型名 |
| api_thinking_disabled | true | 关闭思考模式 |
| api_max_tokens | 4096 | 最大生成长度 |
| api_temperature | 1.0 | 温度（0-2） |
| bot_port | 8080 | 默认端口 |
| auto_port | true | 自适应端口 |
| port_range | [8080, 9090] | 端口扫描范围 |
| superusers | [] | 管理员QQ号列表 |
| napcat_path | "" | NapCat 可执行文件路径 |
| napcat_auto_start | false | 自动启动NapCat |
| text_command_format | "hash" | 文档指令格式 |
| log_level | "INFO" | 日志级别 |
| chat_enabled | true | AI聊天总开关 |

### 8.2 管理员配置

在 config.json 中设置：
```json
"superusers": ["123456789"]
```
管理员可使用 /boring 控制、/add_doc、/del_doc 等管理命令。

---

## 九、部署与使用

### 9.1 快速开始

1. 安装依赖：`pip install -r requirements.txt`
2. 配置 API Key：编辑 config/config.json，填入 api_key
3. 启动 NapCat 并登录 QQ
4. 启动 Bot：`python3 main.py`
5. 在 QQ 中测试：私聊发消息或群聊@机器人

### 9.2 打包为 exe

在 Windows 上双击 `build_exe.bat`，产物在 `dist/QQBot.exe`。

### 9.3 常见问题

- **Bot 启动后收不到消息**：检查 NapCat 反向 WebSocket 地址是否与 Bot 端口一致
- **AI 回复"API Key未配置"**：在 config.json 中填入有效 api_key
- **图片发不出去**：确认 picture/ 或 emotional/ 目录下有对应文件
- **好感度不变化**：确认 api_key 已配置（好感度计算需要调用API）

---

## 十、扩展开发指南

### 10.1 新增插件

在 plugins/ 目录下新建 .py 文件，NoneBot 自动加载：

```python
from nonebot import on_command
from nonebot.adapters.onebot.v11 import MessageEvent

my_cmd = on_command("mycmd", priority=10, block=True)

@my_cmd.handle()
async def _(event: MessageEvent):
    await my_cmd.finish("这是我的新命令！")
```

### 10.2 新增性格

在 personality.py 的 PRESET_PERSONALITIES 中添加，或通过 GUI/命令添加自定义性格。

### 10.3 新增主动消息

编辑 boring.txt，用 % 分隔新句子，发送 /boring reload 重载。

---

*文档结束*
