# QQ Bot 依赖清单
# 核心框架
nonebot2>=2.3.0
nonebot-adapter-onebot>=2.2.0

# Web 驱动
fastapi>=0.100.0
uvicorn>=0.23.0

# HTTP 客户端（AI API 调用）
httpx>=0.24.0

# 可选：GUI 界面（tkinter 为 Python 内置，无需安装）
# 如遇 tkinter 缺失，Ubuntu/Debian 执行: sudo apt install python3-tk
